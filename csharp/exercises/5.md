# TODO Sequence

## Web API application

- Create a web API application and reuse the projects you created in the previous exercise.
- Implement a restfull API with the following endpoints:
    - '/' -> List (Get)
        - With query options (e.g '?completed=2') # same number as defined in the enum
            ```csharp
                // If you're using controllers
                public IActionResult Get(TodoState state) {
                    var filtered = ... get from db using the service (items.Where(x => x.State == state).ToList())
                    return Ok(filtered);
                }
            ```
    - '/' -> Add (Post)
    - '/{id}' -> Update (Put)
    - '/{id}' -> Delete (Delete)