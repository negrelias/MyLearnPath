# TODO Sequence

## Console with classes and DB of your choice but in separated projects

- Separate responsabilities for the application
- Create a `.csproject` to manage the data access (Todo.Data)
- Create a `.csproject` with entities (Todo.Domain)
- Create a `.csproject` with services (Todo.Services)
- Create a `.csproject` with console implementation (Todo.Console)